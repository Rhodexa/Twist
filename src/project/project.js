// A project will later replace "just" scene.
// Project also will contain information about the state of the workspace.

const project = {
    camera: {
        x: 0,
        y: 0,
        width: 1920,
        height: 1080,
        output: {
            w: 1920,
            h: 1080
        }
    }
}

export default project